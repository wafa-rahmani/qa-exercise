{
  "name": "qa-exercise-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "cucumber-js && node support/generate-report.js"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^11.0.0",
    "@playwright/test": "^1.45.0",
    "multiple-cucumber-html-reporter": "^3.7.0"
  }
}
